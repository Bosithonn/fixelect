### What's new in 1.1.1
- **Fixed:** downloading a model on macOS failed with `CERTIFICATE_VERIFY_FAILED`. Downloads and update checks now use your system's trusted certificates, on Mac and Windows.
- The Mac disk image now explains how to open Fixelect the first time, replacing the helper script that newer macOS versions block.
